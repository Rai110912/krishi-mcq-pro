{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "krishi-mcq-pro",
      "runtimeExecutable": "firebase",
      "runtimeArgs": ["serve", "--only", "hosting", "--port", "5000"],
      "port": 5000
    },
    {
      "name": "krishi-source-preview",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-u", "-c", "import os, http.server, socketserver; port=int(os.environ.get('PORT','5500')); socketserver.TCPServer.allow_reuse_address=True; print('serving on', port, flush=True); socketserver.TCPServer(('', port), http.server.SimpleHTTPRequestHandler).serve_forever()"],
      "port": 5500,
      "autoPort": true
    }
  ]
}
